meta {
  name: Search Feeds
  type: http
  seq: 1
}

get {{baseUrl}}/api/search/feeds {
  headers {
    X-API-Key: {{apiKey}}
    Authorization: Bearer {{accessToken}}
  }
  query {
    q: "Test"
    page: 1
    limit: 10
  }
}

tests {
  test("status should be 200", () => {
    expect(res.getStatus()).toBe(200);
  });
}