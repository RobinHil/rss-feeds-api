meta {
  name: Get Article by Link
  type: http
  seq: 3
}

get {{baseUrl}}/api/articles/{{encodedArticleLink}} {
  headers {
    X-API-Key: {{apiKey}}
    Authorization: Bearer {{accessToken}}
  }
}

tests {
  test("status should be 200", () => {
    expect(res.getStatus()).toBe(200);
  });
}