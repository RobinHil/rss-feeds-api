meta {
  name: Remove from Favorites
  type: http
  seq: 3
}

delete {{baseUrl}}/api/favorites/{{feedId}} {
  headers {
    X-API-Key: {{apiKey}}
    Authorization: Bearer {{accessToken}}
  }
}

tests {
  test("status should be 200", () => {
    expect(res.getStatus()).toBe(200);
  });
}