meta {
  name: Sync Feed
  type: http
  seq: 5
}

post {{baseUrl}}/api/feeds/{{feedId}}/sync {
  headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
    Authorization: Bearer {{accessToken}}
  }
  body {
    {
      "forceSync": true
    }
  }
}

tests {
  test("status should be 200", () => {
    expect(res.getStatus()).toBe(200);
  });
}