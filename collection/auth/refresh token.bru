meta {
  name: refresh token
  type: http
  seq: 3
}

post {
  url: {{request}}://{{host}}:{{port}}/api/auth/refresh
  body: json
  auth: none
}

body:json {
  {
    "refreshToken": "{{refresh-token}}"
  }
}
